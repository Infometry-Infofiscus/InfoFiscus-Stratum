name = "text2sql-github-submit"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

[vars]
# Origin has no path — project Pages uses /UI/ in URL but Origin stays github.io only
ALLOWED_ORIGINS = "https://infometry-infofiscus.github.io,http://localhost:3000"

# Set secret: wrangler secret put GITHUB_TOKEN
# Use a fine-grained PAT: Contents read/write on this repository only.
